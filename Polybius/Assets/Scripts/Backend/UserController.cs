using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/* Script that handles User authentication, retrieving values from the database,
 * storing statistics in the database, etc.
 * Acts as a singleton between all scenes
 * Only can have one instance at a time
 */

public class UserController : MonoBehaviour
{

}
